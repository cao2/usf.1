package platu.expression;

/**
 * Enumeration used by the Expression class to indicate 
 * if an error exists and the type of the error.
 * Types include no error (NONE), division by zero (DIVISIONBYZERO), 
 * and array out of bounds (ARRAYOUTOFBOUNDS).
 */
public enum ExpressionError {
	NONE, // indicates no error
	DIVISIONBYZERO, // indicates a division by zero error has occurred
	ARRAYOUTOFBOUNDS // indicates an array is out of bounds
}
